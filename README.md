# 🚀 Agent Skills

**個人化 Agent Skills 生態系** - 專為 Antigravity / Cursor 等 AI IDE 設計的技能庫與安裝工具。

---

## 📋 目錄

- [快速開始（使用者）](#-快速開始使用者)
- [新增技能（創造者）](#-新增技能創造者)
- [專案結構](#-專案結構)
- [可用技能列表](#-可用技能列表)

---

## 🎯 快速開始（使用者）

### 1. 安裝前準備

請確保已安裝 **Node.js** v18 以上。

```bash
# 檢查版本
node -v
```

### 2. 安裝 BEI CLI 工具

```bash
# 從 GitHub 安裝
npm install -g github:Felix-0901/agent-skills/bei-cli-tool
```

或者 Clone 後本地安裝：

```bash
git clone https://github.com/Felix-0901/agent-skills.git
cd agent-skills/bei-cli-tool
npm install
npm link
```

### 3. 使用方式

在你的**專案目錄**中執行：

```bash
bei
```

依照畫面指示：
1. **選擇安裝環境** → Antigravity / Cursor / 自訂路徑
2. **選擇技能** → 使用空白鍵多選
3. **按 Enter** → 自動下載並部署

### 4. 開始開發

下載完成後，**無需任何設定**！

打開 Antigravity 或 Cursor，直接對 AI 說：
- 「幫我設計一個 Logo」
- 「建立一個 Flutter 頁面」

AI 會自動偵測下載的 Skill 並依規範執行。

---

## ✨ 新增技能（創造者）

### 方法一：使用 Skill Architect（推薦）

1. 用 Antigravity 打開本專案
2. 對 AI 說：「我要建立一個 `nextjs-expert` 技能，要求是使用 App Router 和 Tailwind」
3. AI 會自動在 `.agent/skills/` 建立符合標準的技能檔案

### 方法二：手動建立

在 `.agent/skills/<skill-name>/` 資料夾建立 `SKILL.md`：

```markdown
---
name: "skill-name"
description: "描述此技能何時被觸發"
---

# Goal
技能目標

# Instructions
- 指令 1
- 指令 2

# Constraints
- 限制 1
```

### 發布流程

```bash
# 1. 提交變更
git add .
git commit -m "新增 xxx 技能"
git push

# 2. 更新 CLI 技能列表（bei-cli-tool/index.js）
# 在 availableSkills 陣列中加入新技能
```

---

## 📁 專案結構

```
agent-skills/
├── .agent/
│   └── skills/           # 技能存放目錄
│       ├── flutter-pro/
│       ├── logo-design/
│       └── skill-architect/  # Meta-Skill：生成其他技能
├── bei-cli-tool/         # CLI 安裝工具
│   ├── index.js
│   └── package.json
└── README.md
```

---

## 📦 可用技能列表

| 技能名稱 | 說明 |
|---------|------|
| `flutter-pro` | Flutter App 開發技能 |
| `logo-design` | Logo 設計技能 |
| `skill-architect` | Meta-Skill：用於生成新技能的模板 |

---

## 💡 小提示

- **更新技能**：使用者只需重新執行 `bei` 並選擇技能，會自動覆蓋舊檔案
- **Private Repo**：若設為 Private，需處理 SSH 權限
- **自訂路徑**：選擇「自訂路徑」可安裝到任意位置

---

## 📄 License

ISC
